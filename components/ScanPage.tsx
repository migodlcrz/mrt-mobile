import React from 'react';
import {Text, View} from 'react-native';

const ScanPage = () => {
  return (
    <View
      style={{
        backgroundColor: '#dbe7c9',
        height: '100%',
        justifyContent: 'center',
        alignItems: 'center',
      }}>
      <Text className="text-black font-bold">SCAN</Text>
    </View>
  );
};

export default ScanPage;
