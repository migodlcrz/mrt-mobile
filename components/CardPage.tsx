import React from 'react';
import {Text, View} from 'react-native';

const CardPage = () => {
  return (
    <View
      style={{
        backgroundColor: '#dbe7c9',
        height: '100%',
        justifyContent: 'center',
        alignItems: 'center',
      }}>
      <Text className="text-black font-bold">CARD</Text>
    </View>
  );
};

export default CardPage;
